import { Spinner } from "@/components/ui/spinner";

export default function Loading(){
    return(
        <div>
            {/* ANIME GIF AT RANDOM FROM ARRAY */}
            <Spinner/> Loading
        </div>
    )
}